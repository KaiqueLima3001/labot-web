export const siteConfig = {
  sections: {
    integrations: {
      enabled: true,
      showTitle: true,
      showCounter: true,
      maxItems: 12,
    },

    testimonials: {
      enabled: false,
    },

    pricing: {
      enabled: false,
    },
  },
} as const;